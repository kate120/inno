root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ."
  bin = "./tmp/main"
  include_ext = ["go", "html"]
  exclude_dir = ["tmp"]
  delay = 1000
  poll = true
  poll_interval = 1000


[screen]
  clear_on_rebuild = false